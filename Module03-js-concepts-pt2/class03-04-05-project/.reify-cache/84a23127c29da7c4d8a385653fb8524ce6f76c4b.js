"use strict";var chai;module.link("chai",{default(v){chai=v}},0);var mocha;module.link("mocha",{default(v){mocha=v}},1);var TerminalController;module.link("../src/terminalController",{default(v){TerminalController=v}},2);


const { describe, it } = mocha;
const { expect } = chai;

describe('Test the Terminal Controller', () => {
  expect(true).to.be(true)
})